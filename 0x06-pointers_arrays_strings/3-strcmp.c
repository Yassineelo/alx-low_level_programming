#include "main.h"
#include <string.h>
/**
 * _strcmp - compare two strings
 * @s1: to be comapred with s2
 * @s2: to be compared with s1
 * Return: 0
 */

int _strcmp(char *s1, char *s2)
{
	int result = strcmp(s1, s2);

	return (result);
}
